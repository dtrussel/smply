# SPDX-License-Identifier: Apache-2.0
# cppcheck suppressions. Each entry must say why.
# Comments in this file start with '#' -- cppcheck parses anything else as a
# suppression id. A line containing only '#' is NOT a blank comment line: it
# reads as a suppression with an empty id and aborts the whole run with
# "Failed to add suppression. No id." Use '# ---' to separate paragraphs.

# clang-tidy's modernize-* and readability-* cover style; cppcheck is kept only
# for its whole-program value tracking (docs/quality-gates.md section 3).
missingIncludeSystem

# Deliberately non-idiomatic: this file exists to prove strict warnings do not
# reach consumers (tests/consumer/CMakeLists.txt).
*:tests/consumer/consumer_check.cpp

# The `syntaxError:tests/*` suppression that stood here from P7 to P13 is gone.
# It papered over a parser artefact -- cppcheck was run with no include paths
# and no pinned preprocessor configuration, so it explored a Catch2 option
# combination in which TEST_CASE is undefined and the tests do not parse.
# tools/lint.sh now supplies both, and the tests parse. Nothing is suppressed
# for include/, src/ or tests/ any more beyond the two entries above.

# ---
# Dependency headers. Giving cppcheck the project's include paths (P13) also
# gives it Catch2's and QCBOR's, and it reports on what it finds there --
# `duplInheritedMember` in catch_message.hpp, for one. Those are not ours to
# fix, and the alternative of withholding the include paths is what produced
# the syntaxError this file used to suppress. Same reasoning as declaring the
# dependencies SYSTEM for clang-tidy (docs/quality-gates.md section 1).
*:*/_deps/*
